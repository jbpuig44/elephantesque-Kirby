title: Site

columns:
  - width: 1/3
    sections:
      recipes:
        type: pages
        parent: site.find("recettes")
        label: Recettes
        layout: list
        template: recipe

  - width: 1/3
    sections:
      photos:
        type: pages
        parent: site.find("photos")
        label: Albums photo
        layout: cards
        template: album

  - width: 1/3
    sections:
      projects:
        type: pages
        parent: site.find("projets")
        label: Projets
        layout: cards
        template: project
